export class DomListener {
   constructor(node) {
      if (!node) {
         throw new Error("No node provided to DomListener")
      }
      this.node = node
   }

   initDomListeners() {

   }

   removeDomListeners() {

   }
}
